providers = ["python"]

[phases.install]
cmds = ["python -m venv /opt/venv", ". /opt/venv/bin/activate", "pip install -r backend/requirements.txt"]

[start]
cmd = ". /opt/venv/bin/activate && uvicorn backend.server:app --host 0.0.0.0 --port ${PORT:-8000}"
